kind: PersistentVolumeClaim
apiVersion: v1
metadata: 
  name: mysql-pv-claim
  namespace: mysql-namespace

spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
